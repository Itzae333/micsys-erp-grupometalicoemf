# ─────────────────────────────────────────────────────────────
# GrupoMetalicoEMF Web — Variables de entorno
# Copia este archivo como .env.local y rellena los valores reales.
# En Vercel configura estas mismas variables desde el dashboard.
# ─────────────────────────────────────────────────────────────

# ── API ───────────────────────────────────────────────────────
# URL base de la API — sin barra al final.
# Desarrollo local: http://localhost:3001/api/v1
# Producción:       https://tu-api.railway.app/api/v1
NEXT_PUBLIC_API_URL="http://localhost:3001/api/v1"
